/**
 * Typed helpers for generated Signal Works schema.
 * Wire `SupabaseClient<Database>` after `npm run gen:types:cli` (requires `supabase login`).
 */
export type {
  Database,
  Json,
  Tables,
  TablesInsert,
  TablesUpdate,
} from "@/types/database";
